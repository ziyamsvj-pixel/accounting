# CHANGELOG — همکاری انسان و هوش مصنوعی

هر تغییر پروژه اینجا به زبان ساده ثبت می‌شود.

## ۲۰۲۶-۰۹-۲۰

**چه چیزی تغییر کرد:** ورود کاربران با ایمیل و گذرواژه فعال شد و سه سطح دسترسی «خواندن»، «ویرایش» و «مدیر» تعریف شد؛ هر کاربر فقط اسناد خودش را می‌بیند و مدیر همه را. صفحه‌های تازه: ورود/ثبت‌نام، فهرست اسناد حسابداری، فرم ثبت سند (تاریخ، شماره، شرح، حساب‌ها، بدهکار/بستانکار با کنترل توازن)، صفحهٔ جزئیات سند به‌همراه بررسی انطباق آن با قواعد ذخیره‌شده، و دفتر کل که از همان اسناد ساخته می‌شود. منوی بالای سایت هم پیوند اسناد و دفتر کل و دکمهٔ ورود/خروج گرفت.

**چرا:** درخواست مالک پروژه برای ورود کاربران، نقش‌ها، اسناد واقعی، دفتر کل و ماشین‌حساب انطباق.

**نیاز به بررسی توسعه‌دهنده؟** بله. آستانه‌ها و قواعد نمونه هنوز با منبع رسمی راستی‌آزمایی نشده‌اند. تأیید ایمیل هنگام ثبت‌نام روشن است، بنابراین کاربر تازه باید لینک تأیید را باز کند. نقش پیش‌فرض هر کاربر تازه «ویرایش» است و در صورت نیاز باید به «خواندن» تغییر کند. دو هشدار امنیتی دربارهٔ توابع نقش هنوز باز است. هنوز هیچ کاربری ثبت‌نام نکرده، بنابراین مسیرهای محافظت‌شده در عمل آزمایش نشده‌اند.

## ۲۰۲۶-۰۹-۰۹

**چه چیزی تغییر کرد:** ذخیره‌سازی داده فعال شد و سه فهرست پایه ساخته شد: منابع رسمی مقررات، قواعد انطباق و مبانی تخصیص هزینه، همراه با نمونه‌های واقعی. سه صفحهٔ تازه به سایت اضافه شد: «قواعد انطباق» (با فیلتر حوزه) و صفحهٔ جزئیات هر قاعده، «منابع مقرراتی» و «تخصیص هزینه» با ماشین‌حساب سهم هر فعالیت. یک سربرگ و پانویس مشترک فارسی هم برای همهٔ صفحه‌ها ساخته شد.

**چرا:** مالک پروژه هر چهار قدم بعدی و استفاده از پایگاه‌دادهٔ واقعی را تأیید کرد.

**نیاز به بررسی توسعه‌دهنده؟** بله در حد بازبینی محتوا. داده‌های واردشده نمونه‌اند و پیش از اتکای عملی باید با منبع رسمی راستی‌آزمایی شوند. ورود کاربران و سرویس پرداخت همچنان غیرفعال است؛ داده‌ها فقط خواندنی و عمومی‌اند.

## ۲۰۲۶-۰۹-۰۷

**چه چیزی تغییر کرد:** همهٔ فایل‌های فعلی پروژه (صفحهٔ سایت، مستندات انطباق، پوشهٔ قوانین پروژه، بررسی خودکار درخواست‌های ادغام و فایل‌های مرجع طراحی) در مخزن `ziyamsvj-pixel/accounting` روی شاخهٔ `main` بارگذاری شد.

**چرا:** مالک پروژه خواست پس از هر بررسی و تغییر، نتیجه در همان مخزن بارگذاری شود، با رعایت قوانین حاکمیتی.

**نیاز به بررسی توسعه‌دهنده؟** خیر. تنها بارگذاری کد انجام شد؛ پایگاه‌داده، ورود کاربران و سرویس پرداخت همچنان غیرفعال است.

## ۲۰۲۶-۰۹-۰۶

**چه چیزی تغییر کرد:** بستهٔ «انطباق و هوش مقرراتی» به پروژه اضافه شد: مستندات در `docs/compliance/`، گزارش بازبینی در `docs/reviews/`، نمونهٔ مدل قواعد به زبان #C در `src-dotnet/`، تست نمونه در `tests/` و طرح جدول‌های پیشنهادی در `database/compliance/`. همچنین صفحهٔ اصلی سایت از حالت پیش‌فرض خارج شد و یک صفحهٔ فارسی «هستهٔ حسابداری — انطباق و مقررات» جای آن را گرفت که معماری، چرخهٔ عمر قواعد و نقشهٔ راه را نشان می‌دهد.

**چرا:** مالک پروژه بستهٔ مستندات را ارسال کرد و خواست در همین مخزن پیاده شود و پروژه به جلو برود.

**نیاز به بررسی توسعه‌دهنده؟** خیر. پایگاه‌داده، ورود کاربران و سرویس پرداخت دست‌نخورده و غیرفعال است؛ فایل‌های #C فقط مرجع طراحی‌اند و اجرا نمی‌شوند.

## ۲۰۲۶-۰۹-۰۵

**چه چیزی تغییر کرد:** پوشهٔ قوانین پروژه (`project-governance/`) به‌همراه سیاست پروژه، وضعیت پروژه، فهرست منابع و دفتر تصمیم‌ها اضافه شد. این فایل تغییرات و یک بررسی خودکار روی درخواست‌های ادغام (Pull Request) هم اضافه شد.

**چرا:** پروژه هیچ‌کدام از این مستندات را نداشت، در حالی که قوانین مخزن حاکمیت آن‌ها را لازم می‌داند.

**نیاز به بررسی توسعه‌دهنده؟** خیر. هیچ بخشی از خود برنامه تغییر نکرده و ظاهر و عملکرد سایت دست‌نخورده است.
